# النشر والتحديث الإجباري عبر GitHub

1. أنشئ مستودع GitHub خاصاً أو عاماً، ثم ارفع ملفات هذا المجلد بدون `build` وبدون أي ملف مفاتيح توقيع.
2. أنشئ مفتاح توقيع Release واحفظه خارج GitHub. لا ترفع ملف `.jks` أو كلمة مروره إلى المستودع.
3. ابنِ APK موقّعاً وارفعه في **GitHub Releases** مع رقم إصدار أعلى من السابق.
4. زوّد التطبيق باسم المستودع النهائي (`owner/repository`) كي يقرأ آخر Release ويتحقق من `versionCode`.
5. عند وجود إصدار أعلى، يعرض التطبيق شاشة تحديث غير قابلة للتجاوز، ثم يفتح ملف APK للتثبيت. يتطلب ذلك أن يسمح المستخدم بالتثبيت من هذا التطبيق في إعدادات Android.

> لا يمكن فرض تثبيت APK صامت على أجهزة Android العادية أو TV Box دون إدارة مؤسسية للجهاز أو صلاحيات النظام. يمكن فرض منع استخدام النسخة القديمة حتى يثبّت المستخدم الإصدار الأحدث.
